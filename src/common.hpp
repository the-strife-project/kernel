#ifndef COMMON_HPP
#define COMMON_HPP

#include <stdint.h>
#include <stddef.h>

typedef size_t PID;

// These are verbose values for "doNotPanic" parameters
#define DO_NOT_PANIC true
#define DO_PANIC     false

#endif
